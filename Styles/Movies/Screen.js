import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        paddingTop: 20
    },
    title: {
        fontSize: 24,
        display: 'flex',
        textAlign: 'center',
        alignItems: 'center',
        marginBottom: 20
    }
});